async function main() {

    // Distribute tokens

    // Deposit tokens to exchange

    // Make orders

    // Cancel Orders

    // Fill orders
}

main().catch((error) => {
  console.error(error);
  process.exitCode = 1;
});
